# Middlewares

Middlewares transverses standardises:

- `asyncHandler`: capture des erreurs async
- `validateRequest`: consolidation des erreurs de validation
- `errorHandler`: gestion centralisee des erreurs API

Role:

- reduire la repetition
- uniformiser la gestion des erreurs et validations
