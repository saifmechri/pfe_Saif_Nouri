# Routes

Les routes definissent le contrat HTTP:

- endpoint + methode (GET/POST/PUT/DELETE)
- validation d entree (`express-validator`)
- auth/roles via middlewares
- delegation vers les controleurs

Fichier pivot:

- `index.js`: enregistre toutes les routes du backend.
